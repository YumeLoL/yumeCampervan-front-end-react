export const locationList = [
    "all location",
    "melbourne",
    "adelaide",
    "sydney",
]

export enum BookingStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    COMPLETED = 'completed',    
    CANCELLED = 'cancelled',
}

export enum BookingSections {
    REQUEST = 'Request',
    UPCOMING = 'Upcoming',
    HISTORY = 'History',
    CANCELLED = 'Cancelled',
}

